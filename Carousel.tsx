import { useState } from 'react'; import { ChevronLeft, ChevronRight } from 'lucide-react'
export function Carousel({images}:{images:readonly string[]}) { const [active,setActive]=useState(0); const step=(n:number)=>setActive((active+n+images.length)%images.length); return <div className="carousel"><img src={images[active]} alt={`Yash and Pranshu, photograph ${active+1}`} loading="lazy"/><button aria-label="Previous photograph" onClick={()=>step(-1)}><ChevronLeft/></button><button aria-label="Next photograph" onClick={()=>step(1)}><ChevronRight/></button><div className="dots">{images.map((_,i)=><button key={i} aria-label={`Show photograph ${i+1}`} aria-current={i===active} onClick={()=>setActive(i)}/>)}</div></div> }
